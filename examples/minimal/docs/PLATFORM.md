# Platform (minimal example)

Shared always-doc for agents.

- **api** owns HTTP endpoints, auth tokens, payments.
- **web** owns UI and calls the API.
- Prefer changes in one product root per commit.
